apiVersion: v1
kind: Service
metadata:
  name: ntfy
spec:
  ipFamilyPolicy: SingleStack
  type: LoadBalancer
  selector:
    app: ntfy
  ports:
  - port: 8081
    name: main-port
    targetPort: 80
---
