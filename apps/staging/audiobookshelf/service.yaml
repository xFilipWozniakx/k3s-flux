apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf-service
spec:
  type: ClusterIP
  ipFamilyPolicy: SingleStack
  ports:
    - name: app
      port: 3005
      protocol: TCP
      targetPort: 13378
  selector:
    app: audioshell 

