apiVersion: batch/v1
kind: CronJob
metadata:
  name: renovate
spec:
  schedule: "@hourly"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: renovate
              image: renovate/renovate:latest
              args:
                - mischavandenburg/pi-cluster
              envFrom:
                - secretRef:
                    name: renovate-env-variable
                    optional: false
                - configMapRef:
                    name: renovate-configmap
          restartPolicy: Never 
